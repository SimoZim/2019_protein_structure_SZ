notes for 01_getdownload:

bin: script for downloading the .pdb-files and to save them in the directory results,
     script for selecting the .pdb-files and save them in a .txt-file in the directory results

data: link from the PDB, 
      link from Biopython

results: the downloaded 50 .pdb-files from PDB,
	 the .txt-file with the selected .pdb-files

01_run.sh: driver script to perform all the required steps of 01_getdownload